extends base
block content 
    form.form-container
        label.section-title(for='title') Title
        input(id='title' value=`${blog.title}` required)

        label.section-title(for='description') Descrition
        textarea(id='description' required)=`${blog.description}`

        label.section-title(for='content') Content
        textarea(id='content' required)=`${blog.content}`

        button.submit(type='submit') Save
    script(type="module" src='/js/edit.js')